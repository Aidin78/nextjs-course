export default function NotFoundPage() {
    return (
        <div id="error">
            <h1>No Found</h1>
            <p>Unfortunately, we could not find the requested article.</p>
        </div>
    )
}